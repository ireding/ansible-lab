---
- name: Deploy MOTD
  hosts: linux
  become: yes

  tasks: 

  - name: Deploy motd template
    template:
      src: templates/motd.j2
      dest: /etc/motd
      owner: root
      group: root
      mode: '0644'
